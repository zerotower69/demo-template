worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    keepalive_timeout  65;

    server {
        listen 80;
        server_name your-domain.com; #替换为自己的域名,如果想本地测试，请修改本地的hosts文件，将your-domain.com指向127.0.0.1

        location / {
            proxy_pass http://nest-app:4000;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header Host $http_host;
            proxy_set_header X-NginX-Proxy true;
            proxy_http_version 1.1;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

#     server {
#         listen 443 ssl;
#         server_name your-domain.com; #替换为自己的域名,如果想本地测试，请修改本地的hosts文件，将your-domain.com指向127.0.0.1;
#
#         # SSL 配置
#         ssl_certificate /etc/nginx/ssl/fullchain.pem;  # 替换为您的证书路径
#         ssl_certificate_key /etc/nginx/ssl/privkey.pem;  # 替换为您的私钥路径
#         ssl_protocols TLSv1.2 TLSv1.3;
#         ssl_ciphers 'HIGH:!aNULL:!MD5';
#     }
}
